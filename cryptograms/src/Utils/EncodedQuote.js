// will return original quote and encoded quote
export default EncodedQuote(quote){
    // use map to count like characters excluding punctuaion.
    // map of char-value(count)
    // count keys of the map
    // genrate random numbers for count-of-keys
    //ex. 6chars = 6 random numbers
    // random numbers = offset
    // make map of char-offsetchar(if offset = offsetchar = another offset char(use new random number)).
    // return new quote include orinal spaces and puncution.

}
